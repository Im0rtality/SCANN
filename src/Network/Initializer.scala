package Network

import java.io.FileInputStream

import Layer.Layer
import play.libs.Json
import scalax.file.Path

class Initializer {
//    def initialize(cache:String, layers: List[Layer]): List[Layer] = {
//        if (Path(cache).exists) {
//            val json = Json.parse(new FileInputStream(cache))
//
//        } else {
//
//        }
//    }
}
